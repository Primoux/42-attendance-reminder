{
  "name": "42-attendance-reminder",
  "version": "1.0.0",
  "description": "Extension Firefox : prévient avant de perdre son logtime à 3h59",
  "private": true,
  "scripts": {
    "test": "node test/parser.test.js && node test/background.test.js",
    "build": "rm -rf web-ext-artifacts && mkdir -p web-ext-artifacts && zip -q -r web-ext-artifacts/42-attendance-reminder.zip manifest.json background.js content.js parser.js popup.html popup.js icon-16.svg icon-48.svg icon-96.svg icon-128.svg && echo 'web-ext-artifacts/42-attendance-reminder.zip prêt'"
  }
}
